import React from 'react';
import { View, Text } from 'react-native';

const DeviceList = () => {
  return (
    <View>
      <Text>This is the Device List Screen</Text>
    </View>
  );
};

export default DeviceList;
